**1)카메라 작동시키기**

- README 파일에 적힌대로 명령어 입력하면 쉽게 작동시킬 수 있음
- 카메라 작동은 되는데 화면이 실제로 띄워지지는 않음. 
- QT UI 만들고 Day1 hw1에서 영상 띄웠던 코드 참고해서 코드 작성해야함.

**2) 카메라 화면 띄우기**

- insta360 repository는 그냥 카메라 노드 publish까지만 실행시켜줌
- subscribe해서 QT UI에 띄우는건 내가 구현해야함.<br>

  **2.1 QT UI 구현하기**
  - UI는 드래그앤드롭으로 편집하는게 쉬우니까 QT에서 만들어놓고 vscode에서 연결시켜줄거임

  - UI 편집 완료함. 이제 mainwindow.cpp, main.cpp 만들고 subscriber 작성해야함

 **2.2 mainwindow.cpp랑 main.cpp**

 - 전에 진행했던 과제 중에 UI 띄우는거랑 ROS2에서 Topic 통신받는거 따로 구현했던 경험 기억해서 ROS랑 UI랑 기능 나눔 -> Subscriber 파일만 구현함. (나머지는 insta360패키지에 있음)

 - 작성하다보니 main.cpp에서는 ROS2 Node를 실행시켜야하기 때문에 ROS 기능 들어가야함

 **2.3 QT UI에 값들 하나하나 전달하기 귀찮으니 구조체 데이터로 만들어서 갱신하기**

 2.4 결국 QT UI에 값을 출력하려면 subscriber랑 연결해야되니까 ROS 기능이 추가될 수 밖에 없음 -> 2.2 철회

 2.5 아무리 구조체를 쓴다고해도 OpenCV를 사용하는게 훨씬 더 효율적이라고 생각함.
 -> subscriber가 카메라 데이터를 바꾸면 cv::Mat으로 바꾸고 OpenCV가 출력하는 형식으로 최종 마무리 

3) HSV 기반으로 노란색 공 검출하기(Day1 hw2 참고)

- src/Distance/define_yellow_ball.cpp 파일 만들어서 여기서 HSV기반 공 검출 및 영역 검출까지 모두 처리할 생각
- define_yallow_ball 파일에서 공 영역 추출인 4번 과정까지 모두 완료

4) 공 영역 추출하기(Day1 hw2 참고)

- 3번 과정에서 모두 해결함

5) 거리 추정 파이프라인 구현

- 방법 A는 그냥 비례식만 반환하면 되니까 함수에 크게 로직 넣지 않음

- 방법 B는 세타를 어떻게 설정해야하는지가 잘 감이 안옴 
  -> 처음에 gpt추천으로 66.7도 정도로 설정해놓고 30cm 띄워놨을 때 값이 5cm~6cm 정도가 나옴. -> 오차 계산해서 23.3도로 수정해서 thata값 고정