camera_node:
  ros__parameters:
    camera_source: "0"       # /dev/video0 -> 보통 노트북 내장 웹캠. 외장 웹캠이면 "1" 등으로 바뀔 수 있음
    use_synthetic: false     # false로 설정해야 실제 웹캠을 엶
    frame_width: 1920
    frame_height: 1080
    publish_rate_hz: 30.0

ui_node:
  ros__parameters:
    display_rate_hz: 30.0